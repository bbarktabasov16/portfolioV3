/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ваши настройки Next.js могут быть добавлены здесь.
  // Например:
  reactStrictMode: true,
  images: {
    domains: ["example.com"],
  },
  // Добавьте другие настройки, которые вам нужны
};

export default nextConfig;